{% macro list(img = 'null', title = 'null', desc = 'null') %}
  <li class="list-item">
    {% if img != 'null' %}
      <div class="list-item__img"><img src="{{ img }}" alt="{{ alt }}"></div>
    {% endif %}
    {% if title != 'null' %}
      <h3>{{ title }}</h3>
    {% endif %}
    {% if desc != 'null' %}
      <p>{{ desc }}</p>
    {% endif %}
  </li>
{% endmacro %}